// Calculating circumference of a circle.
// function accepts one argument, the radius of the circle.
// function returns the circumference as a number
const circum = (radius) => {
    const pi = 3.142;

    let boundary = 2 * pi * radius;

    return boundary;
};